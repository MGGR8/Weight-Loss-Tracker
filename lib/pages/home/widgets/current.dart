import 'package:flutter/material.dart';

class CurrentProgram extends StatelessWidget {
  const CurrentProgram({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: 200,
      decoration: BoxDecoration(
        color: Colors.red,
      ),
    );
  }
}